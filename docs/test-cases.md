---
tags: [testing]
---

# 产品功能自测用例表

> 由 AI 维护。每次重点修改后必须跑一遍，确保核心功能不受影响。

## 用例列表

### 一、后端 API — 认证

| # | 用例 | API | 验证方式 | 预期结果 |
|---|------|-----|---------|---------|
| A1 | 健康检查 | `GET /health` | HTTP 状态码 | 200，返回 `status: ok` |
| A2 | 微信登录 | `POST /auth/login` | 返回 JSON | 成功返回 token |
| A3 | 未登录访问受保护 API | 不带 token 调用 /links | HTTP 状态码 | 401 |

### 二、后端 API — 链接管理

| # | 用例 | API | 验证方式 | 预期结果 |
|---|------|-----|---------|---------|
| L1 | 创建链接 | `POST /links` | 返回 JSON | 成功，含链接 ID |
| L2 | 获取链接列表 | `GET /links` | 返回 JSON | 成功，含 data 数组 |
| L3 | 获取链接详情 | `GET /links/:id` | 返回 JSON | 成功，含完整链接信息 |
| L4 | 更新链接点评 | `PUT /links/:id` | 返回 JSON | 成功 |
| L5 | 删除链接 | `DELETE /links/:id` | 返回 JSON | 成功 |

### 三、后端 API — 感悟管理

| # | 用例 | API | 验证方式 | 预期结果 |
|---|------|-----|---------|---------|
| N1 | 创建感悟 | `POST /notes` | 返回 JSON | 成功，含感悟 ID |
| N2 | 获取感悟列表 | `GET /notes` | 返回 JSON | 成功，含 data 数组 |
| N3 | 更新感悟 | `PUT /notes/:id` | 返回 JSON | 成功 |
| N4 | 删除感悟 | `DELETE /notes/:id` | 返回 JSON | 成功 |

### 四、后端 API — 分类与标签

| # | 用例 | API | 验证方式 | 预期结果 |
|---|------|-----|---------|---------|
| C1 | 获取分类列表 | `GET /categories` | 返回 JSON | 成功，含分类数组 |
| C2 | 创建分类 | `POST /categories` | 返回 JSON | 成功 |
| C3 | 获取标签列表 | `GET /tags` | 返回 JSON | 成功 |

### 五、后端 API — AI 功能

| # | 用例 | API | 验证方式 | 预期结果 |
|---|------|-----|---------|---------|
| AI1 | AI 分类 | `POST /ai/classify` | 返回 JSON | 成功，含 category + tags |
| AI2 | AI 深度解读 | `POST /ai/insight` | 返回 JSON | 成功，含解读结果 |
| AI3 | AI 辅助点评 | `POST /ai/assist-comment` | 返回 JSON | 成功，含点评建议 |

### 六、后端 API — 统计与上传

| # | 用例 | API | 验证方式 | 预期结果 |
|---|------|-----|---------|---------|
| S1 | 获取统计数据 | `GET /stats` | 返回 JSON | 成功，含链接数/感悟数等 |
| U1 | 上传图片 | `POST /upload` | 返回 JSON | 成功，返回图片 URL |

### 七、小程序端 — 核心流程

| # | 用例 | 页面 | 验证方式 | 预期结果 |
|---|------|------|---------|---------|
| M1 | 首页加载 | index | 页面渲染 | 正常显示链接列表 |
| M2 | 添加链接 | add-link | 提交后返回 | 链接保存成功 |
| M3 | 链接详情 | link-detail | 点击进入 | 显示完整信息和 AI 解读 |
| M4 | 感悟编辑 | note-edit | 提交后返回 | 感悟保存成功 |
| M5 | 分类浏览 | category | 点击分类 | 显示对应分类下的链接 |
| M6 | 搜索功能 | search | 输入关键词 | 显示匹配结果 |
| M7 | 我的页面 | mine | 页面渲染 | 显示统计数据和设置 |
| M8 | 剪贴板检测 | 全局 | 打开小程序 | 检测到链接时弹窗提示 |
| M9 | Timeline 视图 | timeline | 页面渲染 | 时间线正常展示 |
| M10 | AI 整理视图 | organize | 页面渲染 | AI 整理内容正常展示 |

---

## 自动化测试脚本

在项目根目录运行：

```bash
node tests/smoke-test.js
```

> 注意：测试脚本尚未创建，待后续开发。

---

## 变更记录

| 日期 | 变更内容 | 新增用例 |
|------|---------|---------|
| 2026-04-08 | 初始用例表，覆盖后端 API 和小程序核心流程 | A1-A3, L1-L5, N1-N4, C1-C3, AI1-AI3, S1, U1, M1-M10 |
